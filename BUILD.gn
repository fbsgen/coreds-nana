config("coreds_config") {
  include_dirs = [ "src" ]
}

source_set("coreds") {
  sources = [
    "src/coreds/nana/ui.h", # depends on nana
    "src/coreds/nana/pager.h", # depends on nana
  ]
  public_configs = [ ":coreds_config" ]
}

